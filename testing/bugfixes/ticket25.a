;ACME 0.97
; regression test for bug introduced in r419 (fixed in r424, see ticket #25):
	*=$1000
		rts

	!bin "/dev/null", 32000
; including a fixed number of bytes from a file only really needs to be done in
; the last pass. unfortunately, by not doing it, the calculation of the outbuf
; size gave the wrong result and then the last pass created a "outbuffer
; overrun" error.
